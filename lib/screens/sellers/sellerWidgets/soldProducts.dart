import 'package:flutter/material.dart';

class SoldProductsWidget extends StatelessWidget {
  const SoldProductsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sold Products Widget'),
    );
  }
}

import 'package:flutter/material.dart';

class ViewRatingsWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('View Ratings Widget'),
    );
  }
}